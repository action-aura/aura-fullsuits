# Aura Owner Control Center — new codebase, not extracted from Action Aura Enterprise.
flask==3.1.3
werkzeug==3.1.6
flask-cors==6.0.0
flask-wtf==1.2.1
psycopg[binary]==3.2.1
sqlalchemy==2.0.32
alembic==1.13.2
argon2-cffi==23.1.0
pyjwt==2.13.0
pyotp==2.9.0
# UI modernization -- renders the MFA enrollment QR code server-side as an
# inline SVG data URI (no client-side JS, no external request, no CDN).
# Pure-Python, zero transitive dependencies.
# Pinned to the SAME version as requirements/base.txt on purpose:
# requirements/development.txt installs both files into one environment, and
# pip's resolver refuses two different exact pins of one package outright
# ("Cannot install segno==1.6.1 and segno==1.6.6 ... ResolutionImpossible"),
# which is how CI's dependency step -- and every job after it -- went red on
# 2026-08-06 when base.txt moved to 1.6.6 for e-invoicing. Both call sites
# (owner/app/auth/routes.py, commercial_runtime/einvoicing/qr.py) use only
# `segno.make(..., error="m")` + `.save(kind="svg")`, unchanged across 1.6.x.
# Move BOTH pins together, never one.
segno==1.6.6
cryptography==50.0.0
gunicorn==22.0.0
jsonschema==4.23.0

# Phase 9.5B-R -- Owner-wide internationalization (English/Arabic, RTL).
# flask-babel wraps the canonical Babel gettext toolchain (extract/update/
# compile) with Jinja/Flask integration -- not an ad-hoc translation engine.
flask-babel==4.0.0
babel==2.18.0
