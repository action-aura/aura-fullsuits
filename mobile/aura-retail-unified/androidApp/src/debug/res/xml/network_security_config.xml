<?xml version="1.0" encoding="utf-8"?>
<!--
    Task 10 (multi-device-sync-foundation), real finding: Android blocks
    cleartext (http://) traffic by default on API 28+ regardless of what
    SyncRelayConfiguration/ExternalApiConfiguration's own Kotlin-level
    validate() permits for local development, confirmed live: a real
    on-device run failed with "NetworkFailure(reason=CLEARTEXT
    communication to 127.0.0.1 not permitted by network security policy)"
    before this file existed. debug-only (androidApp/src/debug/), merged
    only into debug builds; release builds get no networkSecurityConfig
    override at all and keep the OS default (cleartext blocked everywhere),
    matching this module's own established "cleartext HTTP outside
    production requires an explicit local-development host" security
    stance (SyncRelayConfiguration.isExplicitLocalDevelopmentHost). Scoped
    to exactly the same three explicit local-dev hosts that regex already
    allows, never a blanket cleartextTrafficPermitted="true" on the base
    config, which would also weaken real remote hosts.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
