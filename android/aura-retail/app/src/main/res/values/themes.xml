<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Window theme for the native Compose app (DayNight so the pre-compose window
         background matches light/dark). Compose draws the actual MD3 UI. -->
    <style name="Theme.Aura" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
