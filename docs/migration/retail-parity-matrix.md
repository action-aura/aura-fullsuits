# Aura Retail — Final Parity Matrix (Phase 2B)

Statuses: **PASS** / **PASS WITH DOCUMENTED LIMITATION** / **FAIL** / **NOT APPLICABLE** / **DEFERRED TO ANDROID PHASE**

| Area | Status | Notes |
|---|---|---|
| Backend (Flask app, routing, blueprints) | PASS | `products/retail/backend/app.py` boots independently, registers `auth_bp`, `retail_bp`, `import_bp`; verified both in-process (pytest) and as a packaged exe. |
| Frontend (POS web UI JS) | PASS WITH DOCUMENTED LIMITATION | `subsystem-retail.js` copied byte-identical and confirmed servable (`/static/subsystem-retail.js` → 200). No HTML host page/template exists yet to actually mount it in a browser (`templates/index.html`'s generic subsystem-shell loader was never extracted — this was flagged in the Phase 2 report and remains open). API-level and static-serving parity confirmed; full rendered-page UI walkthrough is not yet possible. |
| Database (schema, seed, connections) | PASS | 17-table retail schema (`database/schema.py`) byte-identical to source `init_retail`; verified via a real clean packaged-exe database initialization (fresh `retail.db`, zero rows, confirmed by direct query before any API call). |
| Authentication | PASS | Registry-based login/logout, PBKDF2 hashing, legacy-SHA256 migration, lockout/throttling, per-install secret key — all ported and verified (47/47 security tests, plus a real login against the packaged exe). |
| Authorization | PASS | `mt_require_subsystem('retail')`, per-user `user_permissions`, cross-company data isolation, fail-closed licensing for provisioned tenants — all verified by test and by direct `curl` against the packaged exe (401/403 responses confirmed). |
| POS (sales, cart) | PASS | `retail_api.py` copied verbatim; sale creation, idempotency key, tax-mode-aware totals verified by `retail_pricing_test.py` end to end through real HTTP routes. |
| Products | PASS | CRUD + stock-adjust routes ported verbatim; product import verified end-to-end including in the packaged exe. |
| Categories | PASS | CRUD ported verbatim; import verified (`test_importing_as_categories_routes_to_categories_not_products`). |
| Suppliers | PASS | CRUD ported verbatim; import verified (`test_importing_as_suppliers_routes_to_suppliers`). |
| Inventory (balances, movements, stock-adjust) | PASS | `inventory_balances`/`inventory_movements` tables + adjust-stock route ported verbatim; exercised indirectly by product-import initial-stock tests and the partial-return stock-restoration test. |
| Sales | PASS | See POS row; also covered by `test_sale_without_discount`, `test_sale_with_discount`, multi-line sale test. |
| Returns (full / partial / multi-line) | PASS | `test_partial_return`, `test_full_return`, `test_multi_line_return` all pass; inventory correctly restored on return. |
| Dashboards (revenue, breakdown) | PASS | `test_month_revenue_calculation`, `test_revenue_becoming_negative_is_not_clamped` (net revenue can go negative, not clamped — matches source), `test_dashboard_breakdown_internally_consistent` all pass unmodified. |
| Reports | NOT APPLICABLE | No dedicated reports module/routes were found in the source Retail implementation beyond the dashboard stats endpoint already covered above — nothing further to extract. |
| Import | PASS | Full Retail-scoped extraction of the source universal import engine (`products/retail/backend/api/import_api.py`): CSV/XLSX/JSON/.db parsing, bilingual (EN/AR) header auto-detection, per-entity cleaning/validation/dedup pipeline, all 5 Retail entities (products/customers/suppliers/branches/categories). 25/25 dedicated tests pass, including against the packaged exe. |
| Export | PASS WITH DOCUMENTED LIMITATION | **No export feature exists in the source implementation** (confirmed by inspection — no export route in `api/import_api.py`, no export call anywhere in `static/js/subsystem-retail.js`). Not a regression introduced by extraction. `test_no_export_endpoint_exists_yet` documents this as a marker for future work rather than fabricating a feature that was never built. |
| Printing | PASS WITH DOCUMENTED LIMITATION | No distinct print implementation (no `window.print()`, no dedicated print route/JS) was found in the source Retail web UI. Flagged for user confirmation in Phase 2's report; unchanged in Phase 2B since no new evidence of an existing print feature was found during this pass. |
| Barcode scanner | PASS | Desktop HID/keyboard-wedge scanner engine (`_scan`/`_scannerDefaults`/`_isDesktopScanner`, `subsystem-retail.js` lines ~584+) copied byte-identical. Android camera-based scanning (`BarcodeScanner.kt`, CameraX + ML Kit) is source-confirmed to exist but is explicitly out of scope until Phase 4 — see Android row below. |
| Localization | PASS | `locales/{en,ar}.json` (139/139 keys, zero untranslated/identical pairs, confirmed real Arabic translations) + `i18n.js` loader (English default, Arabic RTL, localStorage persistence, graceful missing-key fallback, self-contained fetch paths) all migrated and served correctly by the packaged app. Server-side persistence (`POST /api/auth/language`) ported and verified against a real database write. 18/18 dedicated tests pass. Visual RTL layout in an actual rendered browser page is not verified (no browser automation tool in this stack) — see "Frontend" row above for the same underlying host-page gap. |
| Offline operation | PASS | Retail's SQLite-backed, loopback-only architecture requires no network access by design; this property was inherited unchanged (no cloud calls were introduced). Not independently re-tested with network disabled in this pass, since nothing in the extracted code path makes outbound calls except the now-configurable, off-by-default `AURA_EVENT_BUS_URL` (see `retail-extraction-report.md`). |
| Desktop launcher | PASS | `products/retail/desktop/launcher_retail.py` — new file (patterned on source `launcher.py`), verified end-to-end: single-instance guard, free-port scan, waitress server start, clean shutdown via process termination all confirmed working against the real packaged exe. |
| Windows packaging | PASS WITH DOCUMENTED LIMITATION | Real PyInstaller build succeeded; one real bug found and fixed during validation (`sys.frozen` static-path resolution — see `docs/build/retail-windows-build-report.md`). Raw onedir build only: no code signing, no custom icon, no Inno Setup installer wrapper yet. |
| Android | DEFERRED TO PHASE 4 | Untouched in Phase 2B by design (explicitly out of scope for this phase). Source Android project (shared Gradle project, `retail` flavor, `com.actionaura.retail`) was inventoried in Phase 0 but not migrated. Not missing, not broken — scheduled work. |
| Independence from source repo | PASS | Static scan: zero references to the source repo's path or its `api.*`/`core.*`/`database.*` module paths anywhere in `products/retail` or `commercial_runtime`. Dynamic: all 116 Retail tests pass when run with `PYTHONPATH` explicitly emptied (no ambient import path could resolve to the source repo). A full folder-rename isolation test was attempted per the task instructions but was blocked by the environment's safety system (the source repo has pre-existing uncommitted changes unrelated to this work, and renaming the user's primary active repo was correctly flagged as a high-risk, hard-to-justify action) — see `docs/migration/retail-phase-2b-validation-report.md` for the substituted verification method and reasoning. |

## Summary

24 rows total: 18 PASS, 4 PASS WITH DOCUMENTED LIMITATION (Frontend, Export, Printing, Windows packaging), 0 FAIL, 1 NOT APPLICABLE (Reports), 1 DEFERRED TO ANDROID PHASE (Android).
