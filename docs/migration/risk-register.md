# Risk Register — Aura FullSuits Extraction

| ID | Risk | Source | Severity | Mitigation |
|---|---|---|---|---|
| R1 | Weakening the already-hardened auth/secret-key code during extraction (SOURCE fixed a hardcoded-backdoor + SHA-256→PBKDF2 issue in a prior Retail Phase 1 remediation — per session memory and `docs/retail/RETAIL_SECURITY_PHASE_1.md`) | `api/mt_auth.py`, `core/security/app_secret.py`, `core/security/passwords.py` | High | Copy these files verbatim where possible; re-run `retail_security_test.py` (51 tests) unchanged against the extracted copy before any modification |
| R2 | `database/subsystem_db.py` monolith (6,358 lines) is the single biggest coupling point for both products — surgical extraction of `init_retail`/`init_clinic` + shared helpers risks missing a transitively-needed helper function | `database/subsystem_db.py` | High | Extract with tests as the acceptance gate (Phase 2/3), not by inspection alone — if `init_retail()`/`init_clinic()` don't run cleanly against the extracted helper set, the extraction is incomplete, not "close enough" |
| R3 | Clinic → Accounting cross-import (`get_accounting_conn`, raw table writes) is a real functional dependency on an unrelated large-platform module | `api/subsystems/clinic_api.py:517,589` | Medium | Explicit decision required at Phase 3 kickoff: cut the feature, or build a small adapter interface. Must not be silently carried forward as a hidden hard dependency |
| R4 | Hardcoded internal event-bus call assumes same-process port 5000 | `api/subsystems/clinic_api.py:29` | Low-Medium | Remove or make configurable during Phase 3; standalone Clinic won't have this endpoint |
| R5 | Prescription `items_json` is Python `str()`-serialized, not real JSON (`ast.literal_eval` needed to read, not `json.loads`) | `api/subsystems/clinic_api.py:673-675` | Low (data-integrity/tech-debt, not security) | Flag explicitly in `clinic-extraction-report.md`; fix only as an isolated, tested, documented corrective commit per the brief's "no silent behavior changes" rule — do not fix silently while extracting |
| R6 | Stack traces returned to client on `create_patient` failure (`traceback.format_exc()` in JSON response) | `api/subsystems/clinic_api.py:106-108` | Medium (info disclosure) | Fix during extraction as a documented corrective commit (matches brief's "no stack traces returned to clients" security requirement for the new system) |
| R7 | Likely-broken f-string producing a constant/guessable `secret_key` in a code-download generator | `api/system_download.py:123` | N/A to this extraction (file is not ported — Retail/Clinic don't use `system_download.py`) | No action needed for Aura FullSuits; flagged to user as a live SOURCE bug worth a separate fix, out of this task's scope |
| R8 | Hardcoded fallback secret literal in Docker Compose (`aura-enterprise-super-secret-change-in-prod`) | `docker-compose.yml:14` | Medium | New `deployment/docker/docker-compose.production.yml` (Phase 15) must require the env var with no literal fallback |
| R9 | One Retail-discovery research pass was cut off mid-run by an account/session usage limit; its findings were reconstructed via direct grep/read rather than the agent's own structured pass | Phase 0 execution | Low-Medium (process risk, not code risk) | Items 1-17 in `source-inventory.md` should be spot-re-verified at Phase 2 kickoff rather than treated as exhaustively confirmed; three items (13b/14/15: HID scanner, printing/sharing, import/export) are explicitly marked "not yet located" rather than guessed at |
| R10 | Confusable "Clinic" vs. "Healthcare" modules (`clinic_api.py`/`clinic.db` vs `healthcare_api.py`/`healthcare.db`) — a naive grep for "healthcare" during later phases could accidentally pull in the wrong, unrelated module | `api/subsystems/healthcare_api.py`, `database/healthcare.db` | Medium (scope-creep risk) | `healthcare_api.py` is explicitly out of scope; documented prominently at the top of `source-inventory.md`. Confirm with user if it should stay excluded permanently |
| R11 | Android currently stages the *entire* enterprise Python backend (including HR/CRM/Accounting/AI code) into both the Retail and Clinic APK flavors, unused and unlicensed | `android/app/build.gradle:18-19,188-234` | Medium (bloat + potential unintended code exposure in shipped APKs, not currently a security bypass since `_is_module_enabled` still gates access) | Phase 4 must rewrite the staging block to include only `products/retail`, `products/clinic`, `commercial_runtime` — verify final APK size drops and module-gating behavior is unchanged |
| R12 | `DEFAULT_USERS` in `config.py` — 7 hardcoded demo accounts with plaintext password `pass123` | `config.py:196-204` | Medium if ever active in a commercial build | Confirm this dev-seed path is disabled by default in Retail/Clinic production configs (`IS_DEMO_MODE`/`IS_STANDALONE` gating) during Phase 2/3 |
| R13 | Committed dev `config.json` at SOURCE root contains a real-looking `admin_password_hash` | `config.json` (repo root) | Low (it's a dev sample, and `mt_auth.py` already documents not trusting it for licensing) | Do not port this file into `aura-fullsuits`; use `.env.example`/template patterns instead (source-inventory #35) |
| R14 | No existing CI, no existing Clinic test suite — "definition of done" testing requirements (Phase 17) have a real coverage gap to fill, not just port | `tests/` (repo root) | Medium (schedule risk, not correctness risk yet) | Budget explicit time in Phase 3/17 for authoring new Clinic tests and a CI workflow — don't assume porting alone satisfies the testing requirements |
| R15 | Overall task scope (19 phases incl. a full Owner Control Center with Postgres, auth, billing, telemetry, and Android production builds) is large enough that available execution capacity (agent/session limits observed already in Phase 0) constrains how much can be completed per working session | Process | Medium | Sequence phases with checkpoints (see `extraction-plan.md` "Execution note on pacing"); do not attempt to silently skip steps or fabricate completion to fit a single session |
