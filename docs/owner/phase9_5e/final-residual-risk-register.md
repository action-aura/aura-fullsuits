# Phase 9.5E — Final Residual Risk Register

Every item here is accepted, non-blocking, and was consciously scoped during the phase — not an oversight discovered after the fact. Cross-referenced from `phase9-5e-final-decision.md` and `phase9-5e-gate-matrix.md`.

| # | Risk | Severity | Scope | Mitigation / why accepted |
|---|---|---|---|---|
| 1 | `pytest` 8.3.2 known CVE (`PYSEC-2026-1845`) | Low | Dev-tooling only | UNIX-only local-privilege issue; this environment is Windows; `pytest` is never bundled into the frozen `AuraOwner.exe`/`AuraOwner-Dev.exe`. Same accepted residual documented by every prior Owner phase. |
| 2 | M22 real-browser validation is representative, not exhaustive | Low | UI/UX | Golden path, both live segregation-rule rejections, financial-integrity guardrails, cash-closing formula, dashboard/drill-down consistency, and RTL/overflow at all 4 viewports were driven live. The remaining `operations_ui` routes share the same template base/CSS/i18n foundation already proven correct, and are structurally covered by M16/M17's dedicated route-manifest and hardcoded-string-scanner tests. |
| 3 | Cash-closing reopen not completed end-to-end in the browser | Low | UI/UX | The synthetic M22 browser account has no enrolled MFA device, so the live pass proved the `recent_auth_verified` gate redirects correctly but stopped there. The service-layer path with `recent_auth_verified=True` is fully covered by M20/M21's automated test suite. |
| 4 | M23's EXPLAIN ANALYZE pass measured the specific gap it found, not every index | Low | Performance | The `management_notes` index gap was measured directly at 20k-row scale (5.6ms→2.6ms). Two other "Seq Scan" results were reviewed and judged already-optimal given their selectivity, not additional gaps requiring indexes. |
| 5 | Scheduled report generation is in-process/CLI-triggered, not a distributed OS-level scheduler | Low (by design) | Architecture | Deliberate Milestone 1 decision (`operational-finance-duplication-risk-report.md`) to avoid repeating the `.autosync` OS-level-scheduled-task blast-radius class this session already audited. Must not be described as production-grade distributed scheduling until Phase 9R deploys and validates the actual production execution topology. |
| 6 | Attachment file bytes live on the filesystem, not in Postgres | Low (by design) | Backup/restore | `app.system.backup.create_backup()`/`restore_backup()` back up the database only; a real M26 test proved every `ExpenseAttachment` *row* (metadata: content type, hash, storage key) survives backup/restore, but the underlying file bytes are outside a `pg_dump`-based backup's scope by design, matching the existing, unmodified backup architecture from prior phases. |
| 7 | `management_notes.manage` RBAC correction is new-this-phase, not inherited | None (resolved) | RBAC | Found and fixed within this same phase's own closure (see `management-notes-manage-rbac-correction.md`); re-verified via a full 972-test regression and a re-run preflight (50 OK/0 FAIL). Listed here for completeness, not as an open risk. |
| 8 | Test-count claims mid-phase were twice corrected | None (resolved) | Process | 876→879 (baseline) and 73→65 (mid-phase) corrections, both documented additively in `test-count-reconciliation.md` per this phase's own standing instruction never to silently overwrite prior evidence. The final, authoritative count (972) was independently re-verified via fresh `pytest --collect-only` evidence immediately before this closure. |

No item in this register is rated Medium or High. No item blocks the final tag.
