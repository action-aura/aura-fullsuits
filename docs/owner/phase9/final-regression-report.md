# Phase 9 Milestone 19 — Final Regression

Fresh run, from the exact final HEAD (`aa85757`, clean tree), not a reused earlier-session total.

## Results

| Suite | Command | Result |
|---|---|---|
| Owner | `pytest owner/tests -q` | **423/423** (405 Phase 8 baseline + 18 new Phase 9 tests: 5 health/readiness, 4 security headers, 7 observability/redaction, 2 scheduler locking) |
| `commercial_runtime` | `pytest commercial_runtime -q` | **235/235** (unchanged from Phase 8V-P9 — no source in this package changed this phase) |
| Retail | `python products/run_all_tests.py retail` | **194/194**, 12/12 files, canonical ordering-independent runner (Milestone 2) |
| Clinic | `python products/run_all_tests.py clinic` | **135/135**, 11/11 files |

**Total: 987/987 passing. Zero failures. Zero unexplained results.**

## Retail canonical suite — the specific gate this phase closed

Milestone 2's requirement — "the canonical complete Retail suite passes in one clean process,
ordering-independent" — is satisfied by `products/run_all_tests.py`, the repository's own pre-existing
canonical runner (root-caused and formally adopted this phase, not newly built). One command, one
exit code, 12/12 files pass every time, regardless of collection order (each file gets its own fresh
subprocess). Raw monolithic `pytest products/retail/tests` remains order-dependent (proven a
pre-existing, non-functional test-tooling artifact, not a product defect — `retail-test-isolation-
root-cause.md`) and is not the supported command.

## Infrastructure checks

- Migration: real, clean, 6-revision chain against the real `aura_owner_staging` database (Milestone 6).
- Health/readiness: real, tested (`owner/tests/test_health.py`, part of the 423).
- Backup: real, tested (Milestone 7 restore drill).
- Restore drill: real, PASS, 2-second measured restore time (Milestone 7).
- Scheduler: real, tested (`owner/tests/test_scheduled_ops_locking.py`, part of the 423; live-run
  proof in `scheduled-operations.md`).
- TLS/firewall: NOT VERIFIED — no real host (Milestones 4, 12).
- Secret scan: real (Milestone 10) — `pip-audit`/`bandit` both re-run clean after remediation.
- Image scan: NOT APPLICABLE — no container built this session (no Docker Engine).
- SBOM: real, generated (Milestone 10), 94 components.
- Dependency scan: real — `pip-audit` **0 remaining vulnerabilities**, re-confirmed this session after
  the 32-CVE remediation (Milestone 10).
- Staging smoke tests: real (Milestone 18) — liveness/readiness/login-lockout/scheduler-under-load/
  graceful-restart, against a real waitress-served `StagingConfig` Owner process.

## Zero P0, zero P1

Confirmed by the security scans (Milestone 10) and by this regression run itself — no test failure,
known or unexplained, exists at the time of this report.

## No totals reused without an actual final run

This report's numbers were generated by the three commands above, run in this exact session, after
this phase's last source change (`aa85757`) — not copied from the Milestone 10 mid-phase run (which
happened to produce the identical totals, expected since no test-relevant source changed between
Milestone 10 and this final run — Milestones 11-18 were documentation, CI-definition, and
scratchpad-only test scripts, none of which touched `owner/app`, `commercial_runtime`, or any product
test file).
