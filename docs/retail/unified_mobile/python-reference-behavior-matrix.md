# Aura Retail Unified Mobile — Python Reference Behavior Matrix (M3.0)

Per-rule record, exact shape required by the governing spec: Python source file, function/service, inputs, output, rounding, validation, mutation, error behavior, current tests, Kotlin target, parity status. Cross-references `python-financial-authority-map.md` (narrative), `financial-invariant-catalog.md` (invariant list), and `financial-error-code-map.md` (error codes) rather than repeating their full detail.

| Python source | Function | Inputs | Output | Rounding | Validation | Mutation | Error behavior | Current tests | Kotlin target | Parity status |
|---|---|---|---|---|---|---|---|---|---|---|
| `core/retail/pricing.py` | `calculate_line` | `unit_price, quantity, discount_pct=0, tax_rate=0, mode` | `{gross, discount_amount, taxable_amount, tax, total}` | Each field independently `Decimal.quantize('0.01', ROUND_HALF_UP)` | `discount_pct` clamped `[0,100]`; unknown `mode` -> default; no quantity/price validation inside this pure function (caller's job) | None (pure) | Never raises (clamps/falls back) | `retail_pricing_test.py` Part A, 11 tests | `financial/CalculateLine.kt` | LEGACY_PARITY |
| `core/retail/pricing.py` | `calculate_invoice` | `subtotal, discount_amount, tax_rate_pct, mode` | `{discount_amount, taxable_amount, tax, total}` | Same as above | `discount_amount` clamped `[0, subtotal]` | None (pure) | Never raises | `retail_pricing_test.py:114-119` | `usecases/ApplyOrderDiscount.kt` | LEGACY_PARITY |
| `core/retail/pricing.py` | `clamp_discount_pct` | any numeric-ish value | `float` in `[0,100]` | n/a | Negative->0, >100->100, malformed->0 | None | Never raises | Implicit (exercised via `calculate_line`) | `financial/Quantity.kt`/`Discount.kt` clamp helper | LEGACY_PARITY |
| `core/retail/pricing.py` | `normalize_mode` | any value | `"after_discount"` \| `"before_discount"` | n/a | Falls back to default on any unrecognized value | None | Never raises | `retail_pricing_test.py:85-91` | `financial/TaxMode.kt` | LEGACY_PARITY |
| `api/retail_api.py:614` | `create_sale` (quantity parse) | `item['quantity']` (client string/number) | parsed `float` or 400 | n/a | `float(x)` catches `TypeError`/`ValueError` only — **does not catch NaN/Infinity**, real defect | None | 400 "Invalid quantity." | `wave1c_financial_gate_test.py`, `retail_financial_authority_test.py` (exact test names not yet individually catalogued — file-level coverage confirmed, per-test enumeration deferred) | `financial/Quantity.kt` parse | **CANONICAL_UNIFIED** (NaN/Infinity explicitly rejected) |
| `api/retail_api.py:614` | `create_sale` (stock check) | resolved line quantity, `inventory_balances.quantity_on_hand` | pass or 400 | n/a | `qty > on_hand` (0 if no balance row) | None (check only) | 400 "Insufficient stock..." | Same suite | `usecases/FinalizeSale.kt` | LEGACY_PARITY |
| `api/retail_api.py:614` | `create_sale` (persistence) | resolved lines, payment, customer | `sales`/`sale_items`/`inventory_movements`/`inventory_balances`/`customers` rows | Header totals re-quantized from summed `Decimal` line values | Full per-line validation must pass first | Multi-table write, single transaction (see `transaction-boundary-audit.md`) | Blanket `except Exception` -> 500 on unexpected failure | `retail_pricing_test.py` Part B, `retail_financial_authority_test.py`, `wave1c_financial_gate_test.py` | `usecases/FinalizeSale.kt` + `data/SaleRepository.kt` | LEGACY_PARITY (persistence shape); **CANONICAL_UNIFIED** for the product-name-snapshot gap (invariant #20) and the idempotency-conflict gap (invariant #12) |
| `api/retail_api.py:875` | `create_return` (remaining-quantity check) | requested qty, `sale_items.quantity`, `SUM(return_items.quantity)` | pass or 400 | Epsilon `0.0001` | Cumulative across all prior returns for that sale+product | None (check only) | 400 with exact have/requested/already-returned message | `retail_returns_wave0_test.py`, `retail_pricing_test.py:309-386` | `usecases/FinalizeReturn.kt` | LEGACY_PARITY |
| `api/retail_api.py:875` | `create_return` (refund calc) | original `sale_items` row (`unit_price`, `discount_pct`, `tax_rate`), return quantity | refund amount via `calculate_line(...).total` | Same as `calculate_line` | N/A (derived, not independently validated) | `returns`/`return_items`/`inventory_balances`/`inventory_movements` rows | Blanket `except Exception` -> 500 | Same suite | `usecases/FinalizeReturn.kt` | LEGACY_PARITY |

## Coverage note (honest, not overstated)

`retail_financial_authority_test.py` (250 lines) and `wave1c_financial_gate_test.py` (264 lines) were confirmed to exist and scoped (file-level) during this audit but not read function-by-function in this pass — their real, individual assertions will be read and mapped into the differential-harness fixtures (M3.5) as those fixtures are built, rather than duplicated into this matrix twice. This matrix's authoritative claims are all backed by `python-financial-authority-map.md`'s direct source-line citations, which were read in full.
